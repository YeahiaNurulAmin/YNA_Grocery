name: yeahia8800

on:
  push:
    branches:
      - main  # or master, whatever your branch is

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: yeahia8800
        run: |
          git remote add yeahia8800 https://x-access-token:${{ secrets.yeahia8800_TOKEN }}@github.com/Account2Username/YNA_Grocery.git
          git push yeahia8800 --mirror
