/**
 * Backwards compatibility re-export for translations.
 * All locales are defined under src/locales for scalable multi-language support.
 */
import { translations as localesTranslations } from "../locales";

export const translations = localesTranslations;
